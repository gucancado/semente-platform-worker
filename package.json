{
  "name": "semente-platform-worker",
  "version": "0.1.0",
  "description": "Multi-tenant worker da plataforma Semente: webhook + REST + MCP",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "migrate": "tsx src/migrate.ts",
    "test": "node --test --import tsx tests/**/*.test.ts",
    "test:watch": "node --test --watch --import tsx tests/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts",
    "import:fireflies": "tsx src/cli/import-fireflies.ts",
    "lua:bootstrap": "tsx src/cli/lua-bootstrap.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "@aws-sdk/client-s3": "^3.1065.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@types/luxon": "^3.7.1",
    "fastify": "^5.0.0",
    "googleapis": "^172.0.0",
    "luxon": "^3.7.2",
    "openai": "^6.42.0",
    "pg": "^8.13.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=24"
  }
}
